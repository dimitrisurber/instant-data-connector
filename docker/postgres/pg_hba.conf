# PostgreSQL Client Authentication Configuration File
# Secure configuration for FDW Data Connector

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     scram-sha-256

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256

# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     scram-sha-256
host    replication     all             127.0.0.1/32            scram-sha-256
host    replication     all             ::1/128                 scram-sha-256

# Docker network connections
host    all             all             172.16.0.0/12           scram-sha-256
host    all             all             192.168.0.0/16          scram-sha-256
host    all             all             10.0.0.0/8              scram-sha-256

# Kubernetes internal network (adjust based on your cluster configuration)
host    all             all             10.96.0.0/12            scram-sha-256
host    all             all             10.244.0.0/16           scram-sha-256

# Application-specific connections
host    instant_connector  connector_user    0.0.0.0/0        scram-sha-256

# Monitoring connections
host    postgres        monitoring      0.0.0.0/0            scram-sha-256

# Backup connections
host    all             backup_user     0.0.0.0/0            scram-sha-256

# SSL connections (more secure for production)
hostssl all             all             0.0.0.0/0            scram-sha-256

# Deny all other connections
host    all             all             0.0.0.0/0            reject